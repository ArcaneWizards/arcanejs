# Clock Sync Status Example

This example enables toolkit clock sync and renders a custom frontend component
that reads `timeDifferenceMs` and `lastPingMs` from `StageContext`.

To run locally:

- Clone this repository
- Run `pnpm install && pnpm build` in the repo root
- Run `pnpm --filter @arcanejs/examples-clock-sync-status start`
