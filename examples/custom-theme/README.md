# Custom Theme Example

This example shows how to customise ArcaneJS styling by using a custom frontend entrypoint and passing colourful `styled-components` themes to `startArcaneFrontend`.

## Run

```bash
pnpm --filter @arcanejs/examples-custom-theme start
```

Open <http://localhost:1340>.
